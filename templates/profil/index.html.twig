;{% extends 'base.html.twig' %}

{% block title %}Hello ProfilController!{% endblock %}

{% block body %}
{#<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>#}

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    <p><a href="{{ path('dashboard') }}">Retour au dashboard</a></p>

    <div class="profil flex">
        <div class="informations">
            <h2>Mes informations</h2>
            <ul>
                <li>Nom : {{ app.user.lastname }}</li>
                <li>Prénom : {{ app.user.firstname }}</li>
                <li>E-mail : {{ app.user.email }}</li>
                <li>Mot de passe : {{ app.user.password }}</li>
            </ul>

            <p>Modifier mes infos : <a href="{{ path('edit_profil', {'id': app.user.id}) }}">ici</a></p>
        </div>
    </div>
</div>
{% endblock %}
